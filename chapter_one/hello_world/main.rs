// https://doc.rust-lang.org/book/ch01-02-hello-world.html
// Rust is an ahead-of-time compiled language, like C and C++

// We can enforce code formatting by running the command: rustfmt main.rs
fn main() {
    // Rust indents with four spaces
    // println! calls a Rust macro, "!" means we're calling a macro instead of a function
    println!("Hello, world!");
}
